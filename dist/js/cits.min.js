jQuery(document).ready(function($) {
    function heroIsVisible() {
        $("#hero").isVisible() ? mainHeader.addClass("is-transparent") : mainHeader.removeClass("is-transparent");
    }
    function autoHideHeader() {
        var currentTop = $(window).scrollTop();
        belowNavHeroContent.length > 0 ? checkStickyNavigation(currentTop) : checkSimpleNavigation(currentTop), 
        previousTop = currentTop, scrolling = !1, heroIsVisible();
    }
    function checkSimpleNavigation(currentTop) {
        previousTop - currentTop > scrollDelta || currentTop < topOffset ? mainHeader.removeClass("is-hidden") : currentTop - previousTop > scrollDelta && currentTop > scrollOffset && mainHeader.addClass("is-hidden");
    }
    function checkStickyNavigation(currentTop) {
        var secondaryNavOffsetTop = belowNavHeroContent.offset().top - secondaryNavigation.height() - mainHeader.height();
        previousTop >= currentTop ? currentTop < secondaryNavOffsetTop ? (mainHeader.removeClass("is-hidden"), 
        secondaryNavigation.removeClass("fixed slide-up"), belowNavHeroContent.removeClass("secondary-nav-fixed")) : previousTop - currentTop > scrollDelta && (mainHeader.removeClass("is-hidden"), 
        secondaryNavigation.removeClass("slide-up").addClass("fixed"), belowNavHeroContent.addClass("secondary-nav-fixed")) : currentTop > secondaryNavOffsetTop + scrollOffset ? (mainHeader.addClass("is-hidden"), 
        secondaryNavigation.addClass("fixed slide-up"), belowNavHeroContent.addClass("secondary-nav-fixed")) : currentTop > secondaryNavOffsetTop && (mainHeader.removeClass("is-hidden"), 
        secondaryNavigation.addClass("fixed").removeClass("slide-up"), belowNavHeroContent.addClass("secondary-nav-fixed"));
    }
    var mainHeader = $(".cd-auto-hide-header"), secondaryNavigation = $(".cd-secondary-nav"), belowNavHeroContent = $(".sub-nav-hero"), headerHeight = mainHeader.height(), scrolling = !1, previousTop = 0, scrollDelta = 10, scrollOffset = 0, topOffset = 50;
    mainHeader.on("click", ".nav-main-toggle", function(event) {
        event.preventDefault(), mainHeader.toggleClass("nav-open");
    }), $(window).on("scroll", function() {
        scrolling || (scrolling = !0, window.requestAnimationFrame ? requestAnimationFrame(autoHideHeader) : setTimeout(autoHideHeader, 250));
    }), $(window).on("resize", function() {
        headerHeight = mainHeader.height();
    }), $.fn.isVisible = function() {
        var rect = this[0].getBoundingClientRect();
        return (rect.height > 0 || rect.width > 0) && rect.bottom >= 0 && rect.right >= 0 && rect.top <= (window.innerHeight || document.documentElement.clientHeight) && rect.left <= (window.innerWidth || document.documentElement.clientWidth);
    }, heroIsVisible();
});